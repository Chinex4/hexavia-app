import { View, Text } from 'react-native'
import React from 'react'
import ChatScreen from '@/app/(admin)/chats/[channelId]'

export default function AdminChatScreen() {
  return (
    <ChatScreen />
  )
}