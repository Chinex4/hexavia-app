import { View, Text } from 'react-native'
import React from 'react'
import TaskScreen from '@/app/(staff)/(tabs)/tasks'

export default function ClientTaskScreen() {
  return (
    <TaskScreen />
  )
}