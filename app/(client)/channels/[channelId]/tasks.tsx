import { View, Text } from 'react-native'
import React from 'react'
import StatusScreen from '@/app/(staff)/channels/[channelId]/tasks'

export default function ClientChannelTasks() {
  return (
    <StatusScreen />
  )
}